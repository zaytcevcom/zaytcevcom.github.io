import type { Meta } from '@storybook/react';
import { ProfileForm } from 'src/widgets/ProfileForm/ProfileForm';

const meta: Meta<typeof ProfileForm> = {
  title: 'ProfileForm',
  component: ProfileForm,
  tags: ['autodocs'],
};

export default meta;

export const ProfileFormStory = {};
